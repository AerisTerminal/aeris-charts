[package]
name = "origin_engine"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Headless chart model and frame-production state for Origin Charts"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
origin_core = { path = "../origin_core" }
origin_indicators = { path = "../origin_indicators" }
origin_render = { path = "../origin_render" }

[lints]
workspace = true
