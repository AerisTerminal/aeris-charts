[package]
name = "origin_render"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Draw-list IR and rendering math (bar widths, primitives) — backend-agnostic"

[dependencies]
origin_core = { path = "../origin_core" }

[lints]
workspace = true
